using System;

namespace up.fragmentos
{
    class Program
    {
        static void Main(string[] args)
        {
            /** 
             * a. Um programa básico em C# é construído usando
             * classes, colocado em um namespace específico e
             * sempre vai ser iniciado pelo método Main.
             * 
             * b. Os códigos que executam as ações do programa devem
             * ser escrito dentro dos métodos/funções.
             * 
             * c. Além dos tipos primitivos muitas classes já 
             * estão disponíveis no framework .NET. 
             **/

            //Eescrita de texto no console.
            Console.WriteLine("Texto escrito no console!");
            Console.WriteLine("pressione ENTER para sair...");
            Console.ReadLine(); //faz uma pausa.
        }
    }
}
